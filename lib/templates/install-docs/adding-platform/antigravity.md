### Antigravity

Copy your skills into Antigravity's expected directory structure:

```bash
cp -r /path/to/existing/{{name}}/skills .agents/skills/{{name}}/
```

Create `antigravity/package.json` with your plugin metadata (name, version, description, keywords).

Restart Antigravity to pick up the new skills.
